name: Temporary Content Architecture Split v3

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - name: Apply split
        run: bash .github/temporary-content-split.sh
